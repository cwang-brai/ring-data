import Mathlib

theorem generated_90501 : ∀ x : Nat, x + (x + 0) + x + x = (x + x * (0 + 1)) * 1 + x + x := by
  intros x
  ring

theorem generated_90502 : ∀ x y : Nat, x + y = 1 * x + y + 0 := by
  intros x y
  ring

theorem generated_90503 : ∀ x : Nat, 0 + x = x := by
  intros x
  ring

theorem generated_90504 : ∀ x : Nat, (33 + 21) * (x + x) = 54 * (x + x * 1) := by
  intros x
  ring

theorem generated_90505 : ∀ x : Nat, 0 + (x + x) + x + x + (92 + x) * 1 = 1 * (x + x + x) + x + 92 + x := by
  intros x
  ring

theorem generated_90506 : ∀ x y : Nat, x * (1 * (x * 1) + 1 * (y + 0)) + y = x * (x + (0 + y)) + y := by
  intros x y
  ring

theorem generated_90507 : ∀ x : Nat, x + (0 + (0 + x)) + 7 = x + (0 + x + 0) + (1 + (2 + 4) + 0) := by
  intros x
  ring

theorem generated_90508 : ∀ x y : Nat, x * y + 76 = x * (y * (0 + 1)) + 0 + 76 := by
  intros x y
  ring

theorem generated_90509 : ∀ x y z : Nat, x + y + (z + x) = x + (y + z + x) := by
  intros x y z
  ring

theorem generated_90510 : ∀ x y z : Nat, x * 1 + (y + z) = x + (y + z) := by
  intros x y z
  ring

theorem generated_90511 : ∀ x z : Nat, x * (1 * (1 + 0) + 0) + z = 0 + (1 * x + z) := by
  intros x z
  ring

theorem generated_90512 : ∀ y x : Nat, y * (0 + (x + (0 + 0)) * 1 * y + (4 + 96)) = y * (1 * (x * y) + 100 * 1) := by
  intros y x
  ring

theorem generated_90513 : ∀ x : Nat, x * x = 0 + x * (x + 0) := by
  intros x
  ring

theorem generated_90514 : ∀ x : Nat, x + x + 0 = 1 * (x + x * 1 + 0) := by
  intros x
  ring

theorem generated_90515 : ∀ x : Nat, x * 1 = x + 0 := by
  intros x
  ring

theorem generated_90516 : ∀ x y z a : Nat, x * ((x + y + z) * 1 * 1) + y + a = x * (x + y + z) + y * 1 + a := by
  intros x y z a
  ring

theorem generated_90517 : ∀ x y : Nat, 0 + (0 + x) * y = 1 * (x * y) := by
  intros x y
  ring

theorem generated_90518 : ∀ x : Nat, x * x + x = x * x + x := by
  intros x
  ring

theorem generated_90519 : ∀ x y z : Nat, 59 * (x + y + z + (31 + 40)) + 12 = 59 * (((x + y) * 1 + z) * 1 + 71) + 12 := by
  intros x y z
  ring

theorem generated_90520 : ∀ x y : Nat, 1 * ((0 + x + 0) * (y + 0)) = x * y := by
  intros x y
  ring

theorem generated_90521 : ∀ x y : Nat, x + (y * (1 * 1 + 0) + x) = (1 * x + y) * 1 + x := by
  intros x y
  ring

theorem generated_90522 : ∀ x y z : Nat, x + (y + z) + x + 1 * y + 54 = 1 * (x + y + z + 0 + (x + 0 + y)) + 54 := by
  intros x y z
  ring

theorem generated_90523 : ∀ x y : Nat, x * 1 + y = x + y := by
  intros x y
  ring

theorem generated_90524 : ∀ x : Nat, x + 43 + 7 = 1 * x + 1 * (0 + 2 * 25) := by
  intros x
  ring

theorem generated_90525 : ∀ x : Nat, 1 * x * 1 = x + 0 := by
  intros x
  ring

theorem generated_90526 : ∀ x y : Nat, (x + y) * 1 + y = x + 0 + y * (1 * 1) + y := by
  intros x y
  ring

theorem generated_90527 : ∀ x : Nat, x + x = (x + x) * 1 := by
  intros x
  ring

theorem generated_90528 : ∀ x y : Nat, 1 * (x * y) + y + y = 1 * (x * (0 + (0 + y)) * 1) * 1 + (y + y) := by
  intros x y
  ring

theorem generated_90529 : ∀ x : Nat, x * (0 + (x + x)) = x * (x + x + 0) + 0 := by
  intros x
  ring

theorem generated_90530 : ∀ x : Nat, (x * 1 + 0) * x = 1 * (x * x) := by
  intros x
  ring

theorem generated_90531 : ∀ x y z a : Nat, x + y * 1 + z + 0 + y + a * y + z = x + (y + z) + y + a * y + z := by
  intros x y z a
  ring

theorem generated_90532 : ∀ x y : Nat, x * y + y + x = 1 * (x * y) + 0 + y + x := by
  intros x y
  ring

theorem generated_90533 : ∀ x y : Nat, x * y + (0 + y) + x + 22 + 23 = 0 * y + x * y + y + x + (4 + 18) + 23 := by
  intros x y
  ring

theorem generated_90534 : ∀ x y : Nat, 1 * (x + y * 1 * 1) + (54 + 24) + x = x + y + 54 + 24 + x := by
  intros x y
  ring

theorem generated_90535 : ∀ x y : Nat, 83 * (x + y + (10 + 41)) = 83 * (x + (y + (1 + (43 + 0) + 7))) := by
  intros x y
  ring

theorem generated_90536 : ∀ x y z : Nat, 0 + (0 + x) + y + z + 59 = 1 * (0 + (x + y) + z) + 59 := by
  intros x y z
  ring

theorem generated_90537 : ∀ x y z : Nat, x + y + 1 * (0 + z) + 51 + y + 45 = x + y + z + 51 + y + (34 + 11) := by
  intros x y z
  ring

theorem generated_90538 : ∀ x : Nat, x + x = 0 + x + 0 + x := by
  intros x
  ring

theorem generated_90539 : ∀ z x y : Nat, z * (x + y + y) = z * (x + y + y) := by
  intros z x y
  ring

theorem generated_90540 : ∀ x y z : Nat, 1 * x + (y + 0 + (z + z)) = 0 + (x + y * 1 * 1 + z) + z := by
  intros x y z
  ring

theorem generated_90541 : ∀ x y z : Nat, x * ((x * 1 + (0 + y)) * 1 + 0 + z + y) = x * (1 * x + (1 * y + z) + y) := by
  intros x y z
  ring

theorem generated_90542 : ∀ x y z : Nat, x + y + z * 1 = x + (y + z) := by
  intros x y z
  ring

theorem generated_90543 : ∀ x y z : Nat, x + y * 1 + (z + x) + z + 87 = 0 + (x + (y + z)) + x + z + 87 := by
  intros x y z
  ring

theorem generated_90544 : ∀ y x : Nat, (y + x) * (0 + (0 + (x + y))) = (y + x) * (x + y) := by
  intros y x
  ring

theorem generated_90545 : ∀ x y : Nat, x + (y * 1 * 1 + (0 + 0)) + y = x + 0 + y + y := by
  intros x y
  ring

theorem generated_90546 : ∀ y x : Nat, y * (x * 1) = y * ((x + 0) * 1) := by
  intros y x
  ring

theorem generated_90547 : ∀ x y z : Nat, 0 + x + y + z + 1 * (x + 1 * 0) + 76 = (x + y + (z + 0) + (0 + 1 * x)) * 1 + 76 := by
  intros x y z
  ring

theorem generated_90548 : ∀ x y z : Nat, x * (x * 1 + y + z) = x * (x + (y * 1 + z)) := by
  intros x y z
  ring

theorem generated_90549 : ∀ x y z : Nat, x + y + z + 75 = x + y + z + 75 := by
  intros x y z
  ring

theorem generated_90550 : ∀ x y : Nat, 1 * (x * y) + 54 = 1 * x * y + 54 := by
  intros x y
  ring

theorem generated_90551 : ∀ x y z : Nat, x + y + z + 19 = x + y + z + 19 := by
  intros x y z
  ring

theorem generated_90552 : ∀ x y : Nat, x * y + x = x * y + x := by
  intros x y
  ring

theorem generated_90553 : ∀ x : Nat, x * (0 + 1) = 0 + 1 * x := by
  intros x
  ring

theorem generated_90554 : ∀ x : Nat, (x + 0) * (0 + 1 + 0) = 1 * 1 * (0 + x) := by
  intros x
  ring

theorem generated_90555 : ∀ x : Nat, x * (1 * 1 * (1 * 1)) * x = x * x * 1 := by
  intros x
  ring

theorem generated_90556 : ∀ x z : Nat, 1 * (x * 1) + (49 + (z + 0)) = 0 * (1 * 1) + x + (12 + 37) + z := by
  intros x z
  ring

theorem generated_90557 : ∀ x : Nat, x = x + 0 := by
  intros x
  ring

theorem generated_90558 : ∀ x y : Nat, x * (1 * x + 1 * y + x) = x * (x + y + 1 * x) := by
  intros x y
  ring

theorem generated_90559 : ∀ x y : Nat, x * (y + 0) + 66 + 15 + y = x * y * 1 + (11 + 36 + 34) + y := by
  intros x y
  ring

theorem generated_90560 : ∀ x z y : Nat, 0 + x + (x + 18) + z * y = x + (x + 18 + z * y) := by
  intros x z y
  ring

theorem generated_90561 : ∀ x y : Nat, 0 * 1 + (x + y) = 0 + (x + (y + 1 * 0)) := by
  intros x y
  ring

theorem generated_90562 : ∀ x a b : Nat, x * 1 + x + 72 + (a + 10) + b = x + 0 + x * 1 + 9 * 8 + (a + 10) + b := by
  intros x a b
  ring

theorem generated_90563 : ∀ x y z : Nat, x + y + z + 0 + 57 = x + (y + (z + 1 * 0)) + 57 := by
  intros x y z
  ring

theorem generated_90564 : ∀ x : Nat, (x * x + (2 + 78) + x) * 1 = x * x + 20 * 4 + x := by
  intros x
  ring

theorem generated_90565 : ∀ x y a : Nat, x + y + a + y + 0 = x + 1 * (y + a) + y := by
  intros x y a
  ring

theorem generated_90566 : ∀ x y : Nat, (x * (y * 1 * 1) + y * 1) * 1 = (0 + (x * y + y)) * 1 := by
  intros x y
  ring

theorem generated_90567 : ∀ x : Nat, x * x + x + 1 * x = 0 + (x * x + 0) * 1 + x + x := by
  intros x
  ring

theorem generated_90568 : ∀ y x : Nat, y * x = y * x := by
  intros y x
  ring

theorem generated_90569 : ∀ z x y : Nat, z * (x + y + z + 0 + z) = (0 + z) * (1 * x + y + z + z) := by
  intros z x y
  ring

theorem generated_90570 : ∀ x : Nat, (0 + x) * (x * x * 1 + 0) = x * (0 * x + x * x + 0) := by
  intros x
  ring

theorem generated_90571 : ∀ x : Nat, (x + x) * (x * x) = (x + x) * (x * (x + 0)) := by
  intros x
  ring

theorem generated_90572 : ∀ x y : Nat, (x + (18 + 0)) * ((x * 1 + 0) * 1) + (y + x) = (x + (15 + 3)) * x + 1 * y + x := by
  intros x y
  ring

theorem generated_90573 : ∀ x y : Nat, 0 + 1 * 1 * (x * (1 * y)) = x * (1 * (y * 1)) := by
  intros x y
  ring

theorem generated_90574 : ∀ x : Nat, x * x * 1 + x + x + 96 = ((x + 0) * 1 * (x + 0) + x) * 1 + x + 96 := by
  intros x
  ring

theorem generated_90575 : ∀ x y : Nat, (0 + x + 0) * y = 1 * (1 * x * y) + 0 := by
  intros x y
  ring

theorem generated_90576 : ∀ x z a : Nat, x + z + a = x + z + a := by
  intros x z a
  ring

theorem generated_90577 : ∀ x y : Nat, x + y + (2 + y) = x + (y + ((1 + 1) * 1 * 1 + y)) := by
  intros x y
  ring

theorem generated_90578 : ∀ x y : Nat, (0 + 1) * (0 + (x + y + x)) + 57 = (x + (0 + y) + 0) * 1 + x + 57 := by
  intros x y
  ring

theorem generated_90579 : ∀ x y z : Nat, x + (y + z) = x + y + z := by
  intros x y z
  ring

theorem generated_90580 : ∀ x y : Nat, (0 + x) * y = (x * 1 + 0) * y := by
  intros x y
  ring

theorem generated_90581 : ∀ x y z : Nat, 1 * 1 * (x * y + y) + z = 0 + (0 + (x * y + 0)) + 1 * y + z := by
  intros x y z
  ring

theorem generated_90582 : ∀ x y z : Nat, x + (y + z) = x + y + (0 + 0) + z := by
  intros x y z
  ring

theorem generated_90583 : ∀ x : Nat, (x + 0 + (x + 0) + 0) * 1 * 1 = 1 * (x + x) := by
  intros x
  ring

theorem generated_90584 : ∀ x y a : Nat, 1 * x * y + y + 0 + a = x * y + y + a := by
  intros x y a
  ring

theorem generated_90585 : ∀ x z : Nat, x * 1 + (z + (15 + 30)) + x = (x + 0) * 1 + (z + 45) + 1 * x := by
  intros x z
  ring

theorem generated_90586 : ∀ x a y : Nat, x * ((0 + (x + a * (1 + 0))) * (x * y) * 1) = x * ((x + a) * (x * (y + 0)) + 0) := by
  intros x a y
  ring

theorem generated_90587 : ∀ x y b : Nat, ((0 + x) * y + (5 + (68 + 0) + b)) * 1 = x * y + 73 * 1 + b := by
  intros x y b
  ring

theorem generated_90588 : ∀ x : Nat, 0 + (0 + 1 * x + x + 0 + 0) = 0 + x + x := by
  intros x
  ring

theorem generated_90589 : ∀ x : Nat, 1 * ((x * 1 + 0) * 1) = 0 + x * 1 := by
  intros x
  ring

theorem generated_90590 : ∀ y x z : Nat, (y + y) * (y * (1 * (x + (0 + y) + z))) + 59 = (y + y) * (y * (x + y + z + 0)) + 59 := by
  intros y x z
  ring

theorem generated_90591 : ∀ x : Nat, x + (x + x) = x * (1 + 0) + (x + x) := by
  intros x
  ring

theorem generated_90592 : ∀ x y : Nat, x * y + 91 * x = (x + 0) * y + 91 * x * 1 := by
  intros x y
  ring

theorem generated_90593 : ∀ x y z : Nat, x + (y + z) + y = 0 + x + y + z + y := by
  intros x y z
  ring

theorem generated_90594 : ∀ x y : Nat, x * y + (0 + 37) = x * (0 + y) * 1 + (32 + 1 * 5) + 0 := by
  intros x y
  ring

theorem generated_90595 : ∀ x y : Nat, 0 + (1 * (x * 1 + 0) + y) = x + y := by
  intros x y
  ring

theorem generated_90596 : ∀ x y z a : Nat, x + y + (0 + z) + 0 + z + (a + y) + x = 0 + (0 + (x + y + z)) + z + (a + y) + x := by
  intros x y z a
  ring

theorem generated_90597 : ∀ x : Nat, x + (6 + 13 + 0) = 1 * (x + 0) + (19 + 0) := by
  intros x
  ring

theorem generated_90598 : ∀ x y : Nat, x * (x + y + x + 0 + x + y) = x * (x + 0 + y + x + x + y) := by
  intros x y
  ring

theorem generated_90599 : ∀ x y z a : Nat, x + (y + (z + 0)) + 0 + (x + a) * 1 = 1 * (x + y) + z + x + a := by
  intros x y z a
  ring

theorem generated_90600 : ∀ x : Nat, 0 + (0 + x + x + 0) + 69 = 1 * (0 + x) + x + 69 := by
  intros x
  ring

theorem generated_90601 : ∀ x y : Nat, 0 + x * (y * 1) + x + x = (1 * x + 0) * y * 1 + x + x := by
  intros x y
  ring

theorem generated_90602 : ∀ x : Nat, x + (0 + (x + 0 + 0)) = 1 * (x * 1 + 0 + x) := by
  intros x
  ring

theorem generated_90603 : ∀ x : Nat, x * x = x * x := by
  intros x
  ring

theorem generated_90604 : ∀ x a y : Nat, x + 0 + 0 + (a + (y + y)) + y + y = x + a + 0 + y + y + y + y := by
  intros x a y
  ring

theorem generated_90605 : ∀ x : Nat, x + x + (x + 20) + 6 * 1 = x + (0 + x) + x + (20 + 6) := by
  intros x
  ring

theorem generated_90606 : ∀ x y : Nat, (x + (y + 41) + y) * 1 = x + y + 41 + y := by
  intros x y
  ring

theorem generated_90607 : ∀ x y : Nat, x + y * 1 = (0 + (x + (0 + 0) + y)) * 1 := by
  intros x y
  ring

theorem generated_90608 : ∀ x y : Nat, x + 0 + y + (x + y) + x = x + (0 * 1 + (1 * (1 * 0) + y + (x + y))) + x := by
  intros x y
  ring

theorem generated_90609 : ∀ x y : Nat, 0 + x + (y + (0 + 0)) + 0 + 46 = 0 + x + y + 46 := by
  intros x y
  ring

theorem generated_90610 : ∀ x y : Nat, 0 + x + y = x + y := by
  intros x y
  ring

theorem generated_90611 : ∀ x y z : Nat, (x + y * 1 * 1 + 0 + z) * 1 + (23 + 45) + 100 = x + y + (z + 68) + 100 := by
  intros x y z
  ring

theorem generated_90612 : ∀ x y z : Nat, x + y + z + x + 100 + x + z + (92 + x) = 1 * (x + y) + z + (x + 100) + x + z + (92 + x) := by
  intros x y z
  ring

theorem generated_90613 : ∀ x a : Nat, 17 * (0 + (x + 0)) * (0 + 1) + (78 + a) = 17 * x + (78 + a) := by
  intros x a
  ring

theorem generated_90614 : ∀ x : Nat, (0 + x) * (1 * ((0 + 1) * x)) = (0 + 0 + x) * (1 * x) := by
  intros x
  ring

theorem generated_90615 : ∀ x : Nat, 44 * (x + (2 + 0) * 47) = 44 * (0 + (0 + x + 0) + 94) := by
  intros x
  ring

theorem generated_90616 : ∀ x y : Nat, (x + (y + y)) * 1 = x + (y + y) := by
  intros x y
  ring

theorem generated_90617 : ∀ x y z : Nat, x + 1 * y + z + z + y + x = 1 * x + y + z + z + y + x := by
  intros x y z
  ring

theorem generated_90618 : ∀ x y : Nat, x * (0 + y + 0) + (90 + 77) + x = (1 * (1 * 1 * (x + 0)) + 0) * y + 90 + 77 + x := by
  intros x y
  ring

theorem generated_90619 : ∀ x y : Nat, 1 * (x * 1 + (y + 0)) + 0 = x * 1 + 0 + y := by
  intros x y
  ring

theorem generated_90620 : ∀ x y : Nat, x + y + x = x * 1 + (y * 1 + x) := by
  intros x y
  ring

theorem generated_90621 : ∀ x y z : Nat, 0 + 0 + x + (y + z) = 1 * (x + y) + 0 + z := by
  intros x y z
  ring

theorem generated_90622 : ∀ x y z : Nat, 1 * (x * y) + (30 * 2 + z) = x * y + (60 + z) := by
  intros x y z
  ring

theorem generated_90623 : ∀ x y z : Nat, 0 + x + (y + (0 + z)) = 1 * (x + 0 + y * 1) * 1 + z * 1 := by
  intros x y z
  ring

theorem generated_90624 : ∀ x y : Nat, x * y = (0 + (x + 0)) * y := by
  intros x y
  ring

theorem generated_90625 : ∀ x y z : Nat, x + y + 1 * (z * 1 + x) + 99 = x + (0 + y) + z + x + 99 := by
  intros x y z
  ring

theorem generated_90626 : ∀ x : Nat, 1 * 1 * x = x := by
  intros x
  ring

theorem generated_90627 : ∀ x z y : Nat, x * 1 + (0 + 24 + 6 + x * 1) + (z + y) = x + (0 + 30 + x) + (z + y) := by
  intros x z y
  ring

theorem generated_90628 : ∀ x : Nat, x + (x + x) = 0 + 0 + (x + 0) + (x + x) := by
  intros x
  ring

theorem generated_90629 : ∀ x y z : Nat, x + y + z + 0 = x + 0 + y + z := by
  intros x y z
  ring

theorem generated_90630 : ∀ x y z : Nat, x + (1 * y + 1 * z) = 0 + (x + y) + z * 1 := by
  intros x y z
  ring

theorem generated_90631 : ∀ x z : Nat, 1 * (x * 1) + z + (z + z) = 0 + 1 * (x + z) + (z + z) := by
  intros x z
  ring

theorem generated_90632 : ∀ x : Nat, 0 + x * 1 = x := by
  intros x
  ring

theorem generated_90633 : ∀ x : Nat, x + x = x + x := by
  intros x
  ring

theorem generated_90634 : ∀ x y z a : Nat, 0 + (x + (y + 0)) + z + z + 0 + a = x + 0 + (y + (z + z)) * 1 + a := by
  intros x y z a
  ring

theorem generated_90635 : ∀ x : Nat, x + 10 = x + 0 + 5 * 2 := by
  intros x
  ring

theorem generated_90636 : ∀ x y : Nat, x * y + x = 1 * (x * y) + 1 * x := by
  intros x y
  ring

theorem generated_90637 : ∀ x y z : Nat, x + (1 * y + (0 + (0 + 1)) * z + y) = (x + (y + z)) * 1 + 0 + y := by
  intros x y z
  ring

theorem generated_90638 : ∀ x y z a : Nat, x + y + z + a + z = x + y + z + a + z := by
  intros x y z a
  ring

theorem generated_90639 : ∀ x y z : Nat, x + y + 1 * z + (6 + 63) = 0 + x + y + (z + (17 + 1 + 51)) := by
  intros x y z
  ring

theorem generated_90640 : ∀ z x y : Nat, 1 * z * (x * y) + 0 = z * ((x + 0) * y * 1) := by
  intros z x y
  ring

theorem generated_90641 : ∀ y x z : Nat, y * (1 * (1 * x + y) + z) = y * (0 + x + y + z) := by
  intros y x z
  ring

theorem generated_90642 : ∀ x y : Nat, x + y * 1 = x + 1 * (0 + y) := by
  intros x y
  ring

theorem generated_90643 : ∀ x y : Nat, x + (0 + y) = x + 1 * (y + 0) := by
  intros x y
  ring

theorem generated_90644 : ∀ x : Nat, x * x + (1 + 0) * x + (x + x) + (x + x) = x * (0 + 1 * x) + x + x + x + (x + x) := by
  intros x
  ring

theorem generated_90645 : ∀ x a : Nat, x * (1 * 1) + a + a + 3 = x + 0 + a + a + 3 := by
  intros x a
  ring

theorem generated_90646 : ∀ x y : Nat, x * 1 * y + y = 1 * (1 * (x * ((0 + y) * 1))) + y := by
  intros x y
  ring

theorem generated_90647 : ∀ x y : Nat, x + y + x + (8 + 87) = x + (y + x) + 95 := by
  intros x y
  ring

theorem generated_90648 : ∀ x : Nat, x + x + (8 + x) = x + (x * 1 + (8 + 0 + x)) := by
  intros x
  ring

theorem generated_90649 : ∀ x y a : Nat, x * y + (1 * a + x + a) = 0 + (x * (1 * y) + a + x + a) := by
  intros x y a
  ring

theorem generated_90650 : ∀ x z y : Nat, x * 1 + (z + y) = 0 + (x + 0 + (z + y)) := by
  intros x z y
  ring

theorem generated_90651 : ∀ x y : Nat, 1 * (x + y) = x + y * 1 * 1 := by
  intros x y
  ring

theorem generated_90652 : ∀ x a : Nat, x + a * 1 = (0 + x) * 1 + a := by
  intros x a
  ring

theorem generated_90653 : ∀ x : Nat, x + x + x = x + x + x := by
  intros x
  ring

theorem generated_90654 : ∀ y x z : Nat, (19 + 17 + y) * (0 + x + 0 + y + 0 + z) + x = (0 + 36 + y) * (x + y + z) + x := by
  intros y x z
  ring

theorem generated_90655 : ∀ a x z : Nat, a * (x + 0 + z) = a * (x + (0 + 0 + z)) := by
  intros a x z
  ring

theorem generated_90656 : ∀ x y : Nat, x + y + y = 0 + (x + y + y) := by
  intros x y
  ring

theorem generated_90657 : ∀ x y z a : Nat, 0 + (x + y) * 1 + (z + a) + a + y = 1 * (0 + x + y + z + a + a) + y := by
  intros x y z a
  ring

theorem generated_90658 : ∀ x y : Nat, x + (0 + y + 51) = x + y + (35 + 16) + 0 := by
  intros x y
  ring

theorem generated_90659 : ∀ x y z : Nat, x + y + z = 1 * (0 + (0 + x + y)) + z := by
  intros x y z
  ring

theorem generated_90660 : ∀ x y z : Nat, 0 + (x * (1 * y) + (0 + 0)) + z = x * (0 * 1 + (y + 0) + 0) + z := by
  intros x y z
  ring

theorem generated_90661 : ∀ x y z : Nat, x + y + z + y = x + y + z + y := by
  intros x y z
  ring

theorem generated_90662 : ∀ x y : Nat, x + (y + x) = 1 * (1 * x) + (y + x) := by
  intros x y
  ring

theorem generated_90663 : ∀ x y z : Nat, x * y * 1 + 0 + y + z = 1 * (x * y + y) * 1 + z := by
  intros x y z
  ring

theorem generated_90664 : ∀ x : Nat, x + 22 + 2 + (64 + x) = x + (16 + 0 + 6) + 2 + (64 + x) := by
  intros x
  ring

theorem generated_90665 : ∀ x y : Nat, x * y + x = x * y + x := by
  intros x y
  ring

theorem generated_90666 : ∀ x y : Nat, 0 + 1 * x + (0 + y) + y = x + (y + y) := by
  intros x y
  ring

theorem generated_90667 : ∀ x z : Nat, x + 0 + (0 + z) + 32 = 0 + (x * 1 + z) + 32 := by
  intros x z
  ring

theorem generated_90668 : ∀ x y : Nat, 0 + (x + y) * (0 + 1) = x + y * 1 := by
  intros x y
  ring

theorem generated_90669 : ∀ x : Nat, x + x = x + x := by
  intros x
  ring

theorem generated_90670 : ∀ x y z : Nat, 0 + ((0 + x) * 1 + y + 0 + z + x) = (1 + 0) * (x + y) + z + x := by
  intros x y z
  ring

theorem generated_90671 : ∀ x y z a : Nat, 0 + x + y + z + 93 + a * z = 0 + (x + (0 + (0 + y)) + z) + 93 + a * z := by
  intros x y z a
  ring

theorem generated_90672 : ∀ x y : Nat, x * y + 0 + (62 + 1 * (y * y)) + 3 = x * y + 2 * 31 + y * y + 3 := by
  intros x y
  ring

theorem generated_90673 : ∀ x y : Nat, (0 + x) * y + (y + 12) = x * y + (y + 12) := by
  intros x y
  ring

theorem generated_90674 : ∀ x y : Nat, x * y + (74 + y) = x * y + (74 + y) := by
  intros x y
  ring

theorem generated_90675 : ∀ x y : Nat, x + (y + (x + x)) + y = x + 0 + y + (x + x * 1) + y := by
  intros x y
  ring

theorem generated_90676 : ∀ x y z : Nat, x * y + z = x * ((0 + 1) * y) + z := by
  intros x y z
  ring

theorem generated_90677 : ∀ x : Nat, x + (x * 1 + x) = 0 + x + (x * 1 + 1 * x) := by
  intros x
  ring

theorem generated_90678 : ∀ x y : Nat, x * y + 39 = (x + 0) * y + 39 := by
  intros x y
  ring

theorem generated_90679 : ∀ x a : Nat, 100 * (x + a) = 100 * (x + 0 + 0 * (1 + 0) + a) := by
  intros x a
  ring

theorem generated_90680 : ∀ x y z : Nat, (x + y) * 1 + z + y = x + y + z + y := by
  intros x y z
  ring

theorem generated_90681 : ∀ x y a : Nat, x * (0 + (0 + y)) + a + a + (19 + 1) + x + x = x * (y + 0) + a + a + 20 + x + x := by
  intros x y a
  ring

theorem generated_90682 : ∀ y x : Nat, y * (x + y * 1) = y * ((0 + x) * 1 + y) := by
  intros y x
  ring

theorem generated_90683 : ∀ x y z : Nat, (1 + 0) * x * y + (x + z) = (0 + (0 + (0 + x))) * y + (x + z) := by
  intros x y z
  ring

theorem generated_90684 : ∀ x : Nat, x = 0 + x := by
  intros x
  ring

theorem generated_90685 : ∀ x : Nat, x + (0 + 1 * x) = x + x := by
  intros x
  ring

theorem generated_90686 : ∀ x y : Nat, 1 * (x + y) + 22 = x + (y + 22 * 1) := by
  intros x y
  ring

theorem generated_90687 : ∀ x : Nat, x * 1 + (x + 1 * (x * x)) = x + 0 + 1 * x + x * x := by
  intros x
  ring

theorem generated_90688 : ∀ x y : Nat, x + (y + (0 + 10)) + (0 + y) + y = 0 + (x + y) + 0 + (0 + 10) + y + y := by
  intros x y
  ring

theorem generated_90689 : ∀ x y : Nat, x * (1 * 1) * y = (x + 0 * 1) * y := by
  intros x y
  ring

theorem generated_90690 : ∀ x y z : Nat, x + y + z + x = x + y + z + x := by
  intros x y z
  ring

theorem generated_90691 : ∀ x y a : Nat, x * y + 55 + a = x * (1 * y) + 55 + a := by
  intros x y a
  ring

theorem generated_90692 : ∀ x y : Nat, (0 + x) * y + (0 + x) + (x + y) = x * y + x + x + y := by
  intros x y
  ring

theorem generated_90693 : ∀ x y : Nat, x * y + y + (y + 59 * 1) = x * y + 0 * y + (y + 0) + (y + (33 + 26)) := by
  intros x y
  ring

theorem generated_90694 : ∀ x : Nat, x * (1 + 0) * 1 + 0 * 1 = x := by
  intros x
  ring

theorem generated_90695 : ∀ x y : Nat, 1 * x + (y + 13 * 1) + y = x + y + (4 + 9) * (1 * 1) + y := by
  intros x y
  ring

theorem generated_90696 : ∀ x y : Nat, x + y + y = x + y + y := by
  intros x y
  ring

theorem generated_90697 : ∀ x y : Nat, x * 1 + y + (x + y) = 1 * x + y + (x + (0 + y * 1)) := by
  intros x y
  ring

theorem generated_90698 : ∀ x y z : Nat, x * (y * 1 * 1) + 69 * 1 + y + z = x * y + (60 + 9 * 1) + y + z := by
  intros x y z
  ring

theorem generated_90699 : ∀ x y z : Nat, 1 * (x * 1) + (y + (z + 0 + (14 + 48))) = x + y + (z + (47 + 15)) := by
  intros x y z
  ring

theorem generated_90700 : ∀ x y : Nat, 0 + (x + y) + (y + y) = x + y + y + 1 * y := by
  intros x y
  ring

theorem generated_90701 : ∀ x : Nat, x * x = 1 * x * x := by
  intros x
  ring

theorem generated_90702 : ∀ x : Nat, 1 * x + (0 + 1 * x) + (x + 92) = (x + x) * 1 + (x + 92) := by
  intros x
  ring

theorem generated_90703 : ∀ x a : Nat, 1 * (x + 0) + a = 1 * x + a := by
  intros x a
  ring

theorem generated_90704 : ∀ x y z : Nat, (0 + 1) * (x + y) + z = x + y + z := by
  intros x y z
  ring

theorem generated_90705 : ∀ x y : Nat, 0 + (x * (y * 1) + 57) = x * (y * 1) + (6 + (5 + 46)) := by
  intros x y
  ring

theorem generated_90706 : ∀ y z x : Nat, (1 * y + z) * (x + y * 1 + z) = (y + z) * (x + y + z) := by
  intros y z x
  ring

theorem generated_90707 : ∀ x : Nat, 0 + x + 0 = x := by
  intros x
  ring

theorem generated_90708 : ∀ x y : Nat, 1 * x * y + y = x * y * 1 + y := by
  intros x y
  ring

theorem generated_90709 : ∀ x : Nat, x * 1 = x := by
  intros x
  ring

theorem generated_90710 : ∀ x y z : Nat, (x + y + z) * 1 = x + 0 + y + z := by
  intros x y z
  ring

theorem generated_90711 : ∀ z x y : Nat, 9 * (z * (x * (0 + y + 0)) + y) = 9 * (z * (1 * x * (0 + y)) + y) := by
  intros z x y
  ring

theorem generated_90712 : ∀ x : Nat, x + (36 + 27) = x + 63 := by
  intros x
  ring

theorem generated_90713 : ∀ x a : Nat, x + (a + 0) = (0 + 0) * 1 + x + a := by
  intros x a
  ring

theorem generated_90714 : ∀ x y : Nat, x + y + x = x + (y + x) := by
  intros x y
  ring

theorem generated_90715 : ∀ x y : Nat, x * y + x * 1 + (y + 49) = x * y + x + (y + 49) := by
  intros x y
  ring

theorem generated_90716 : ∀ x : Nat, 0 + x + 93 * 1 = 1 * 1 * 1 * x * 1 + 93 := by
  intros x
  ring

theorem generated_90717 : ∀ x y : Nat, (0 + x) * (y + 1 * 0) + y = 1 * (x * y) + y := by
  intros x y
  ring

theorem generated_90718 : ∀ x y z : Nat, x + y + z + z = x + y + z + z := by
  intros x y z
  ring

theorem generated_90719 : ∀ x y a : Nat, x + y + 1 * 16 + a = 1 * (0 + x + y) + (2 + 6) * 2 + a := by
  intros x y a
  ring

theorem generated_90720 : ∀ x y : Nat, x + (x + y) = x + (x + (y + 0)) := by
  intros x y
  ring

theorem generated_90721 : ∀ x y z : Nat, x * y + z = x * 1 * y * 1 + z := by
  intros x y z
  ring

theorem generated_90722 : ∀ x y z : Nat, 77 * (x * (1 * y)) + z + 0 = 77 * (x * y) + z := by
  intros x y z
  ring

theorem generated_90723 : ∀ x y z : Nat, x + y + z + 59 + 71 + x = x + (y + z) + (19 + 1 * (30 + 10) * 1) + 71 + x := by
  intros x y z
  ring

theorem generated_90724 : ∀ x y : Nat, x + y * 1 + 42 + (35 + y) = x + y + 0 + (0 + (10 + 32)) + (35 + y) := by
  intros x y
  ring

theorem generated_90725 : ∀ x z y : Nat, x * ((0 + z) * (x * y + x)) + x + x + z * y = x * (z * (x * y + x)) + (x + (x + z * y)) := by
  intros x z y
  ring

theorem generated_90726 : ∀ x y : Nat, (x + 18) * (x + y) + (71 + 3) = 1 * 1 * (1 * ((x + 18) * (x + y))) + 0 + 74 := by
  intros x y
  ring

theorem generated_90727 : ∀ x : Nat, 1 * x * (0 + x) = x * x := by
  intros x
  ring

theorem generated_90728 : ∀ x y b : Nat, 0 + (1 * (x + y) + b) = x + y + b := by
  intros x y b
  ring

theorem generated_90729 : ∀ z x y : Nat, z * (x * 1 * y) = z * (x * y) := by
  intros z x y
  ring

theorem generated_90730 : ∀ x : Nat, (x + 0) * x + 1 * x * x = x * (x + x) := by
  intros x
  ring

theorem generated_90731 : ∀ x y : Nat, 1 * 1 * (0 + (0 + x * 1 * 1 * 1) * y) = x * y * 1 + 0 * 1 := by
  intros x y
  ring

theorem generated_90732 : ∀ x a : Nat, 0 + x + a = x + a := by
  intros x a
  ring

theorem generated_90733 : ∀ x : Nat, x * 1 = x := by
  intros x
  ring

theorem generated_90734 : ∀ x y z : Nat, x * 1 + (y + 0) + z + x + x + y = x + y + (z + x) + 0 + x + y := by
  intros x y z
  ring

theorem generated_90735 : ∀ x y z : Nat, x + 1 * y + z = x * 1 + (0 + 1) * y * 1 + z := by
  intros x y z
  ring

theorem generated_90736 : ∀ x y : Nat, x * 1 + y + 12 = 0 + x * 1 + y + 12 := by
  intros x y
  ring

theorem generated_90737 : ∀ y x : Nat, y * (x * y + 0) = y * (1 * (x * (0 + y))) := by
  intros y x
  ring

theorem generated_90738 : ∀ x y : Nat, (0 + x) * y = 1 * x * (1 * (y * 1)) := by
  intros x y
  ring

theorem generated_90739 : ∀ x : Nat, x * x + (x + x) = x * (x * 1) + (x + x) + 0 := by
  intros x
  ring

theorem generated_90740 : ∀ y x : Nat, y * (1 * (0 + x * y)) = (y + (0 + 0)) * ((0 + x) * (1 * y)) := by
  intros y x
  ring

theorem generated_90741 : ∀ x y : Nat, x + y * 1 + (y + 10) = x + y + (y + 10) := by
  intros x y
  ring

theorem generated_90742 : ∀ x y : Nat, 1 * (x * (1 * 1 * y)) + x = (x + 0) * y + x := by
  intros x y
  ring

theorem generated_90743 : ∀ x y : Nat, x + (0 + y) + (x + 0) = x + 1 * y + x := by
  intros x y
  ring

theorem generated_90744 : ∀ x b z : Nat, 0 + x * (1 * 1) + x * 1 + (b + z) = x + x + (b + z) := by
  intros x b z
  ring

theorem generated_90745 : ∀ x y : Nat, x + y + y = 0 + x + (y + y) := by
  intros x y
  ring

theorem generated_90746 : ∀ x y : Nat, x + 0 + (y + 19 * 2) + 20 = 0 + x + y + 38 + 20 := by
  intros x y
  ring

theorem generated_90747 : ∀ x y : Nat, x + y + (0 + x) = x + y + x := by
  intros x y
  ring

theorem generated_90748 : ∀ x : Nat, 0 + (x + 1 * (0 + 0) + 0) = x + 0 := by
  intros x
  ring

theorem generated_90749 : ∀ x y a : Nat, 0 + (x * 1 + (y + 0)) + a + y + (x + 18) = 0 + x + (y + 0) + a + y + (x + 18) := by
  intros x y a
  ring

theorem generated_90750 : ∀ x y z : Nat, 0 + (x + y) + 0 + z = x + (y + z) := by
  intros x y z
  ring

theorem generated_90751 : ∀ x : Nat, x * (1 * x) = x * x := by
  intros x
  ring

theorem generated_90752 : ∀ z x y a : Nat, z * ((x + y + z) * 1 + x + (a + 96) + 24) = z * (x + y + z + (x + 0) + (a + 96) + 24) := by
  intros z x y a
  ring

theorem generated_90753 : ∀ x y : Nat, x + 1 * y + 0 = x + y + 0 := by
  intros x y
  ring

theorem generated_90754 : ∀ x y : Nat, x + (y + x) = 0 + x + y + x := by
  intros x y
  ring

theorem generated_90755 : ∀ x : Nat, x * x + (0 + 0) + x + 44 + 47 = x * x + x + 44 + 0 + 47 * 1 := by
  intros x
  ring

theorem generated_90756 : ∀ x : Nat, x + 0 + 0 + 0 = x + 0 := by
  intros x
  ring

theorem generated_90757 : ∀ x y z : Nat, x + y + z + (z + z) = x + y + z + (z + z) := by
  intros x y z
  ring

theorem generated_90758 : ∀ x : Nat, 98 * (0 + x) = 98 * (x + 0) := by
  intros x
  ring

theorem generated_90759 : ∀ x y z : Nat, x * y + z = 1 * (x * (0 + y * 1)) + z := by
  intros x y z
  ring

theorem generated_90760 : ∀ x y z : Nat, (0 + 1) * (x + 0) + y + (z + 0 + 2 * 29) + z = 0 + (x + y + z + 58) + 0 + z := by
  intros x y z
  ring

theorem generated_90761 : ∀ x y : Nat, (0 + 1) * (0 + (1 + 0)) * (x * (0 + y)) = x * y * (1 * 1) := by
  intros x y
  ring

theorem generated_90762 : ∀ x : Nat, (x + 0) * 1 + 16 + 89 = x + 0 * 1 + 16 + 89 := by
  intros x
  ring

theorem generated_90763 : ∀ x y : Nat, x + (0 + 1) * (y * 1 * 1) = 0 + (x + y) := by
  intros x y
  ring

theorem generated_90764 : ∀ x y : Nat, x * 1 * y + (y + x) + x = 1 * (1 * x * (y * 1)) + (y + x) + x := by
  intros x y
  ring

theorem generated_90765 : ∀ x y z : Nat, (x + (1 * y + z) + z + (34 + 22)) * 1 = 0 + (0 + x) + (y + z) + z + 56 := by
  intros x y z
  ring

theorem generated_90766 : ∀ x : Nat, 1 * (x + (0 + (x + 0) * x)) + x = x + x * x + x := by
  intros x
  ring

theorem generated_90767 : ∀ x y : Nat, x * y + 0 + 0 * 1 = 0 + x * 1 * y + 0 := by
  intros x y
  ring

theorem generated_90768 : ∀ x : Nat, x + (x + x) = x + (0 + x + (x + 0)) := by
  intros x
  ring

theorem generated_90769 : ∀ x y : Nat, x * y + 0 + (x + (0 + x)) = x * 1 * y + x + x := by
  intros x y
  ring

theorem generated_90770 : ∀ x : Nat, 0 + x * 1 + 0 + 0 + 0 = x + 0 := by
  intros x
  ring

theorem generated_90771 : ∀ x y : Nat, 0 + 1 * (x + y) + 0 = x + y := by
  intros x y
  ring

theorem generated_90772 : ∀ x y : Nat, x * 1 * y + (0 + y) = x * y + y := by
  intros x y
  ring

theorem generated_90773 : ∀ x y : Nat, x * (y * (1 * 1 * 1)) + 47 + 99 = x * y + 47 * 1 + (0 + 99) := by
  intros x y
  ring

theorem generated_90774 : ∀ x y z : Nat, x + 0 + y + 1 * z + y + y + z = x + y + z + y + y + 0 + z := by
  intros x y z
  ring

theorem generated_90775 : ∀ x : Nat, x + 31 = 0 + x + 31 := by
  intros x
  ring

theorem generated_90776 : ∀ x y z : Nat, 0 + (x + y) + z + x = x + (0 + y) + (0 + z + 0) + x := by
  intros x y z
  ring

theorem generated_90777 : ∀ x y : Nat, 0 + (x + y) = x + y := by
  intros x y
  ring

theorem generated_90778 : ∀ x y a : Nat, 0 + 1 * (1 * x) * 1 + (y + a) = x + (y + a) := by
  intros x y a
  ring

theorem generated_90779 : ∀ x y : Nat, 0 + x + y + 44 + (57 + 23) = 1 * x + y + 44 + 80 := by
  intros x y
  ring

theorem generated_90780 : ∀ x y : Nat, x + (y + 0) + 65 + (x + x) = x + y + ((2 + 13) * 1 + 50) + (x + x) := by
  intros x y
  ring

theorem generated_90781 : ∀ y x : Nat, y * (0 + (x + y) + x) = y * (x + (y + 0) + x) * 1 := by
  intros y x
  ring

theorem generated_90782 : ∀ x : Nat, 1 * (x + x) + (x + x) = (x + x + x) * 1 + x := by
  intros x
  ring

theorem generated_90783 : ∀ x y : Nat, x * (0 + y) + (77 + y) = x * y + (77 + y) := by
  intros x y
  ring

theorem generated_90784 : ∀ x : Nat, x + (x + x) + x = x + (x + x * 1) + x := by
  intros x
  ring

theorem generated_90785 : ∀ x b z : Nat, x + (b + z) = 1 * (x + 0) + (b + z) := by
  intros x b z
  ring

theorem generated_90786 : ∀ x y : Nat, 1 * ((x + 0) * (y + 0) + 0) + y + 0 = x * (y * 1) * 1 + y := by
  intros x y
  ring

theorem generated_90787 : ∀ x a z : Nat, 0 * 1 + x + 1 * a + a + (x + z) = x + a + a + (x + z) := by
  intros x a z
  ring

theorem generated_90788 : ∀ x : Nat, 1 * (1 * x) * x = 1 * (x * x) + 0 * 1 + 0 := by
  intros x
  ring

theorem generated_90789 : ∀ x y : Nat, x * y + 65 = x * (y + 0) + 65 := by
  intros x y
  ring

theorem generated_90790 : ∀ x y : Nat, x + y * 1 = x + y := by
  intros x y
  ring

theorem generated_90791 : ∀ x y : Nat, x + y + 0 + 61 = 1 * (x + 1 * 1 * y) + (40 + 21) := by
  intros x y
  ring

theorem generated_90792 : ∀ x : Nat, x + x = 0 + x + x := by
  intros x
  ring

theorem generated_90793 : ∀ x y : Nat, x * y * (1 * 1) = 0 + x * y := by
  intros x y
  ring

theorem generated_90794 : ∀ x y : Nat, 0 + x * 1 * y + (69 + 15) = 0 + x * y + (32 + 52) := by
  intros x y
  ring

theorem generated_90795 : ∀ x y : Nat, x * (1 * y) + y + y + (98 + x) = x * (0 + (1 * 0 + y)) + (1 * y + y) + (98 + x) := by
  intros x y
  ring

theorem generated_90796 : ∀ x y : Nat, 1 * 0 + x * y + (18 + 54) = x * y * 1 + 18 + 54 := by
  intros x y
  ring

theorem generated_90797 : ∀ x y : Nat, x + 0 + y + 30 + 91 = x + 1 * (0 + y + 15 * 2) + 91 := by
  intros x y
  ring

theorem generated_90798 : ∀ x y : Nat, x + y + x * 29 = 0 + x + (0 * 1 + y + x * 29) := by
  intros x y
  ring

theorem generated_90799 : ∀ x : Nat, x * 1 * 1 = x := by
  intros x
  ring

theorem generated_90800 : ∀ x y z : Nat, 0 + 1 * (1 * (x * 1 + y)) + z = x * 1 + y + z := by
  intros x y z
  ring

theorem generated_90801 : ∀ x y : Nat, x * y + y = x * (0 + y) + y := by
  intros x y
  ring

theorem generated_90802 : ∀ x y z : Nat, 1 * x + (y + z + 0 + z * 31) + (x + 57) + z = 0 + x + (y + z) + z * 31 + (x + 57 + 0) + z := by
  intros x y z
  ring

theorem generated_90803 : ∀ x : Nat, x + (x + 0 + 1) = x + x + 1 := by
  intros x
  ring

theorem generated_90804 : ∀ x : Nat, 1 * (x + 3 * 6) = x + (14 + 4) := by
  intros x
  ring

theorem generated_90805 : ∀ x y z a : Nat, x + (0 + 1 * y) + z + (a + a) + 41 = (x + y * 1) * 1 * 1 + z + (a + a) + 41 := by
  intros x y z a
  ring

theorem generated_90806 : ∀ x : Nat, x + x + 0 = 1 * (0 + x + x) := by
  intros x
  ring

theorem generated_90807 : ∀ x y z : Nat, x * (1 * 1) + y + z + 32 = 1 * (x + y) + z + 0 + 4 * 8 := by
  intros x y z
  ring

theorem generated_90808 : ∀ x y z : Nat, x + y + z + y = x + (y + 1 * (1 * 1) * (z + y)) := by
  intros x y z
  ring

theorem generated_90809 : ∀ z x y : Nat, z * (x * y + x) = z * (x * (1 * (1 * y)) + x) := by
  intros z x y
  ring

theorem generated_90810 : ∀ x : Nat, x * x + 1 * (3 + 30) = x * x + 0 * x + (6 + (26 + 1)) := by
  intros x
  ring

theorem generated_90811 : ∀ x a : Nat, 1 * x + a = 0 + x + a := by
  intros x a
  ring

theorem generated_90812 : ∀ y x z b : Nat, y * (x + (y + z)) + y + (62 + b) = y * (x + y) + (y * z + y) + (62 + b) := by
  intros y x z b
  ring

theorem generated_90813 : ∀ x : Nat, 0 + x * 1 = 1 * 1 * x := by
  intros x
  ring

theorem generated_90814 : ∀ x y : Nat, x * (y + 0) = x * 1 * y + (0 + 0) := by
  intros x y
  ring

theorem generated_90815 : ∀ x y a : Nat, x * (y + 0) + a + (10 + 79) = x * (y + 0) + a + 89 := by
  intros x y a
  ring

theorem generated_90816 : ∀ x : Nat, x = 0 + x := by
  intros x
  ring

theorem generated_90817 : ∀ x y : Nat, (0 + x) * 1 + (55 + 43 + y) + (y + x) = x + (98 + y) + (y + x) := by
  intros x y
  ring

theorem generated_90818 : ∀ x : Nat, 1 * (1 * (x * 1) * x + (x + x)) + (x + 70) = x * (x * 1) + x + x + (x + 70) := by
  intros x
  ring

theorem generated_90819 : ∀ x z : Nat, 1 * (x + z) = 0 + (x + z) := by
  intros x z
  ring

theorem generated_90820 : ∀ x : Nat, (68 + 0) * (x + (x + 0) + x) + (x + 44) = 68 * (x + x + (0 + (x + 0))) + (x + 44) := by
  intros x
  ring

theorem generated_90821 : ∀ x y z : Nat, x + y + 1 * (1 * z) = 0 + (0 + (x + y + z)) := by
  intros x y z
  ring

theorem generated_90822 : ∀ x y z : Nat, 1 * ((0 + (x + y + z)) * 1) = 1 * x * 1 + y + z := by
  intros x y z
  ring

theorem generated_90823 : ∀ x : Nat, x + 0 + 81 = x + 81 := by
  intros x
  ring

theorem generated_90824 : ∀ x : Nat, 0 + x = 0 + x := by
  intros x
  ring

theorem generated_90825 : ∀ x y : Nat, x + y = x + (y + 0) := by
  intros x y
  ring

theorem generated_90826 : ∀ x y : Nat, x * 1 * (0 + y) + x = (x + 0 + 0) * y + x := by
  intros x y
  ring

theorem generated_90827 : ∀ x y : Nat, x + (0 + y) = x + 0 + y + 0 := by
  intros x y
  ring

theorem generated_90828 : ∀ x y z : Nat, x * (y * 1) + 0 + x + (x + (81 + 12 + z)) = x * (y * 1) + (x + x) + (93 + z) := by
  intros x y z
  ring

theorem generated_90829 : ∀ x y z a : Nat, 0 + (x + y + 0 + z) + a = 0 + x + y + z + a := by
  intros x y z a
  ring

theorem generated_90830 : ∀ x y z : Nat, x * y + z + 97 = x * y + (1 * z + 97) := by
  intros x y z
  ring

theorem generated_90831 : ∀ x : Nat, x + x = x + (0 + x) := by
  intros x
  ring

theorem generated_90832 : ∀ x : Nat, 0 + x + x + x = 0 + (x + x) + x := by
  intros x
  ring

theorem generated_90833 : ∀ x : Nat, x * 1 + (60 + (91 + 0)) = x + (60 + 91) := by
  intros x
  ring

theorem generated_90834 : ∀ x y : Nat, x + y + 0 = x + y := by
  intros x y
  ring

theorem generated_90835 : ∀ x y z : Nat, x + (y + z) = x + (y + z) := by
  intros x y z
  ring

theorem generated_90836 : ∀ x y z : Nat, x + y * 1 + z = x + (y + 0) + z := by
  intros x y z
  ring

theorem generated_90837 : ∀ x y z : Nat, 1 * (x + 1 * y + z) + 32 = x + (y + z) + 32 := by
  intros x y z
  ring

theorem generated_90838 : ∀ x : Nat, x * (1 + 0) + 93 + (47 + x) = 1 * (1 * x) + 93 + (47 + x) := by
  intros x
  ring

theorem generated_90839 : ∀ z x y : Nat, (z + x + 0) * (y * x) + 31 = (z + x) * (y * x) + 31 := by
  intros z x y
  ring

theorem generated_90840 : ∀ x y z : Nat, 0 + (x + (y + z)) = x + y + z := by
  intros x y z
  ring

theorem generated_90841 : ∀ x y : Nat, 1 * (x * 1 * (0 + y) * 1) + x = (x + (0 + 0)) * (0 + y) + (0 + x) := by
  intros x y
  ring

theorem generated_90842 : ∀ x : Nat, 0 + (1 * 0 + x + (x + x)) + 0 = x + (x + 1 * x) := by
  intros x
  ring

theorem generated_90843 : ∀ x y : Nat, x * y * 1 = (0 + x) * (y * (1 * 1) + 0) := by
  intros x y
  ring

theorem generated_90844 : ∀ x y : Nat, 0 + 0 + x + y = 1 * x * 1 + 0 + y := by
  intros x y
  ring

theorem generated_90845 : ∀ x y : Nat, x * y = x * (y + 0) := by
  intros x y
  ring

theorem generated_90846 : ∀ x : Nat, 1 * x + x = x + 1 * x := by
  intros x
  ring

theorem generated_90847 : ∀ x : Nat, x * (0 + (x + (x + (x + 0)))) + x + (x + x) = x * (x + (0 + x + 0) + x) + x + (x + x) := by
  intros x
  ring

theorem generated_90848 : ∀ x y b : Nat, x * y + y * 1 + x + (0 + b) = x * ((0 + y) * 1) + (y + x) + (0 + b) := by
  intros x y b
  ring

theorem generated_90849 : ∀ x y z : Nat, (x + y * 1) * 1 + (39 + z) = 0 + x + (y + 0) + (39 + z) := by
  intros x y z
  ring

theorem generated_90850 : ∀ x z : Nat, 1 * (x + 0) + 0 + (z + x * 1) = x * 1 + (z + 1 * (1 * 0 + 0) + x) := by
  intros x z
  ring

theorem generated_90851 : ∀ x y : Nat, x + y + 75 + x + x = x + (0 + y) + 25 * 3 * (1 * 1) + x + x := by
  intros x y
  ring

theorem generated_90852 : ∀ x y a : Nat, x + y + (y + a) = x + y + y + a := by
  intros x y a
  ring

theorem generated_90853 : ∀ x : Nat, 1 * x + (x + (x + 0 + (28 + 34)) + (x + 0) * 1) = x + x + (x + 62) + x := by
  intros x
  ring

theorem generated_90854 : ∀ x y : Nat, x * (x + y) + (35 + y) = x * (x + y) + (35 + y) := by
  intros x y
  ring

theorem generated_90855 : ∀ x y z : Nat, x * 1 * 1 + (y + (z + 0)) * 1 = x + 1 * y + z := by
  intros x y z
  ring

theorem generated_90856 : ∀ x z : Nat, x + 97 + 0 + z = x + 97 + z := by
  intros x z
  ring

theorem generated_90857 : ∀ x y : Nat, x + (20 + 4) + y = x + (24 * 1 + 1 * y) := by
  intros x y
  ring

theorem generated_90858 : ∀ x y : Nat, x * (1 * y + 0) + x + y = 1 * (x * 1) * (y + 0) * 1 * (1 * 1) + x + y := by
  intros x y
  ring

theorem generated_90859 : ∀ a x y b : Nat, a * ((x + 0) * y + b) = a * (x * (0 + 0 + y * 1) + 1 * b) := by
  intros a x y b
  ring

theorem generated_90860 : ∀ x y : Nat, x + y = (x + y) * 1 := by
  intros x y
  ring

theorem generated_90861 : ∀ x y : Nat, x + (y + y + 0) + y + (y + x * 1 * 1) = x + y + (y * 1 + y) + (y + x) := by
  intros x y
  ring

theorem generated_90862 : ∀ x : Nat, x = 0 + x := by
  intros x
  ring

theorem generated_90863 : ∀ x : Nat, 0 + x + 56 + 48 = 0 + x * 1 + 56 + 48 := by
  intros x
  ring

theorem generated_90864 : ∀ x y : Nat, 0 + (x * y + 0 + y + y) + y = x * y + (y + y) + y := by
  intros x y
  ring

theorem generated_90865 : ∀ x y a : Nat, (x + y + a) * 1 + a = x + (y + 0 + a) + a := by
  intros x y a
  ring

theorem generated_90866 : ∀ x y : Nat, x + (y + 0) = x + y * 1 + 0 := by
  intros x y
  ring

theorem generated_90867 : ∀ x : Nat, 1 * x = x := by
  intros x
  ring

theorem generated_90868 : ∀ x y : Nat, x + y + (69 + 7) + y = x + (0 + y) + 0 + 76 + 1 * y := by
  intros x y
  ring

theorem generated_90869 : ∀ x : Nat, x + (0 + x + (0 + 0)) + 48 + x = x + (x + 12 * 4 + x) := by
  intros x
  ring

theorem generated_90870 : ∀ x y a : Nat, 1 * (0 + x * y + 0 + y + a + 10 + a) = (x * y + y) * (1 * (1 * 1)) + a + 10 + a := by
  intros x y a
  ring

theorem generated_90871 : ∀ x y z : Nat, x + y + z + y = (x + y) * 1 + z + y := by
  intros x y z
  ring

theorem generated_90872 : ∀ x : Nat, x * x + 56 * x = x * (1 * 0 * 1 + x) + (14 + 9 + 33) * x := by
  intros x
  ring

theorem generated_90873 : ∀ z y x : Nat, (z + (y + 0)) * (1 * (x * y)) + z = (z + y) * (x * y) + z := by
  intros z y x
  ring

theorem generated_90874 : ∀ x y : Nat, x + 0 + (0 + y + x) + (73 + (x + 0)) = 1 * 1 * (x + (y + x)) + (73 * 1 + x) := by
  intros x y
  ring

theorem generated_90875 : ∀ x y : Nat, 0 + x * y + 0 = 1 * 1 * (x * (0 + y)) + 0 := by
  intros x y
  ring

theorem generated_90876 : ∀ x y z a : Nat, x + y + (z + 0) + (a + (z + y)) = x + y + (z + a) + z + y := by
  intros x y z a
  ring

theorem generated_90877 : ∀ x y : Nat, 0 + x + y = x + (y + 0) := by
  intros x y
  ring

theorem generated_90878 : ∀ x y : Nat, x * 1 + (0 + 0) + y + 71 = 1 * 1 * x + 1 * y + 0 + 71 := by
  intros x y
  ring

theorem generated_90879 : ∀ x y z : Nat, 0 + (0 + x + y + z) = 0 + (x + y + (0 + z)) := by
  intros x y z
  ring

theorem generated_90880 : ∀ x : Nat, x + 0 + x + 0 = (x * 1 * 1 + x) * (0 + (0 + 1)) := by
  intros x
  ring

theorem generated_90881 : ∀ x y a : Nat, 1 * (1 * x) * y + a = (0 + x) * 1 * y + a := by
  intros x y a
  ring

theorem generated_90882 : ∀ x y z : Nat, 1 * (1 * (x * y) + z) = (x + 0) * (y + 0) + z := by
  intros x y z
  ring

theorem generated_90883 : ∀ x y z : Nat, x + (y + z + 0 * 1) = (x + y + 0) * 1 + (0 + z) := by
  intros x y z
  ring

theorem generated_90884 : ∀ x : Nat, (x + x) * (1 * 1) + x * 1 = x + (x + (x + 0 + 0)) := by
  intros x
  ring

theorem generated_90885 : ∀ x y : Nat, x * y + y + 7 = x * y + y + (2 + 5) := by
  intros x y
  ring

theorem generated_90886 : ∀ x y b : Nat, (0 + x) * y + b = (0 + x) * (1 * y) + b := by
  intros x y b
  ring

theorem generated_90887 : ∀ x y z a : Nat, x + y + z + (z + a) + 68 = x + (y + z) + (z + a) + 68 := by
  intros x y z a
  ring

theorem generated_90888 : ∀ a x z : Nat, a * (x + z + z) = a * (1 * x + z + 1 * (z + 0)) := by
  intros a x z
  ring

theorem generated_90889 : ∀ x y z a : Nat, 1 * (x * 1 + y) + z + (z + a) = x + (y + 0 + z * 1) + z + a := by
  intros x y z a
  ring

theorem generated_90890 : ∀ x y : Nat, 1 * (1 * (0 + (x * y + y))) + y + x = (0 + x + 0) * (0 + y) + y + y + x := by
  intros x y
  ring

theorem generated_90891 : ∀ x : Nat, x + 0 + 0 = 0 + x := by
  intros x
  ring

theorem generated_90892 : ∀ x y z : Nat, x + y + z = x + y + (z + 0) := by
  intros x y z
  ring

theorem generated_90893 : ∀ x y : Nat, 0 + x + y = 0 + x + (0 + y) := by
  intros x y
  ring

theorem generated_90894 : ∀ x y z : Nat, x + (y + z) + 0 * 1 = 1 * (1 * (1 * 1)) * (x + y) + (z * 1 + 0 + 0) := by
  intros x y z
  ring

theorem generated_90895 : ∀ x y z : Nat, x + y + z + (y + y) = x + y + z + y + y := by
  intros x y z
  ring

theorem generated_90896 : ∀ x : Nat, x * (0 + (0 + 1 * (x * x) + x) + (x + x + x)) = x * (x * x + (0 + x) + (x + x) + x) := by
  intros x
  ring

theorem generated_90897 : ∀ x : Nat, x + x + x + 17 * 5 = x + (0 + (x + x) + (0 + 85)) := by
  intros x
  ring

theorem generated_90898 : ∀ x y : Nat, x + y + (90 + 8) = x + y + (49 + 49) := by
  intros x y
  ring

theorem generated_90899 : ∀ x z : Nat, x + z + x = 0 + x + (z + x) := by
  intros x z
  ring

theorem generated_90900 : ∀ x : Nat, 0 + (x + 0) + (79 + 5 + (x + 0)) = x * 1 + 84 + x := by
  intros x
  ring

theorem generated_90901 : ∀ x y : Nat, x + y = x * 1 + y := by
  intros x y
  ring

theorem generated_90902 : ∀ x : Nat, 1 * (1 * 1) * (1 * x) + x * 1 = x + x := by
  intros x
  ring

theorem generated_90903 : ∀ x : Nat, x * (x + (0 + x + 0)) + x * x + x = x * (x + x) + x * x + x := by
  intros x
  ring

theorem generated_90904 : ∀ z x y : Nat, z * (x * y) * (1 * 1) + (y + x) = z * (1 * x * y) + (y + x) := by
  intros z x y
  ring

theorem generated_90905 : ∀ x y : Nat, 38 * (99 * (x * y + 0)) = 38 * (99 * (x * y)) := by
  intros x y
  ring

theorem generated_90906 : ∀ x a z : Nat, 1 * (x * (1 + 0)) + a + z + 50 = x * 1 + (a + z + (27 + 23)) := by
  intros x a z
  ring

theorem generated_90907 : ∀ x z : Nat, 0 + x + z * 1 + 9 = (0 + 1) * (1 * x + z) + 9 := by
  intros x z
  ring

theorem generated_90908 : ∀ x : Nat, x + x = x + x := by
  intros x
  ring

theorem generated_90909 : ∀ x z : Nat, x + 50 + 0 + 39 + z + x = x + (1 * 89 + z) + x := by
  intros x z
  ring

theorem generated_90910 : ∀ x a : Nat, (0 + x) * 1 + a = 1 * 0 + (x + (1 * a + 0)) := by
  intros x a
  ring

theorem generated_90911 : ∀ x y : Nat, (1 * x + y + 29) * (0 + 1) + y = x + (y + 29) + y := by
  intros x y
  ring

theorem generated_90912 : ∀ x y z : Nat, x * (y * (x + (y + z))) + z = x * (1 * (y * (x + (y + z)))) + z := by
  intros x y z
  ring

theorem generated_90913 : ∀ x : Nat, x + x + (0 + x) + x + (x + x) = x + 0 + x + x + x + x + x := by
  intros x
  ring

theorem generated_90914 : ∀ x : Nat, 22 * (x * 1) = 22 * (0 + (x + 1 * (1 * 0 + 0)) + 0) := by
  intros x
  ring

theorem generated_90915 : ∀ x y z : Nat, 5 * (x + (y + z)) = (0 + 5 + 0) * (x + y + z) := by
  intros x y z
  ring

theorem generated_90916 : ∀ x y : Nat, (x + 0 + y) * 1 + x + (x + y) + 40 = x + (y + (1 + 0) * x) + (x + y) + 40 := by
  intros x y
  ring

theorem generated_90917 : ∀ x y b : Nat, 1 * (0 + (x + 1 * 0 + y + 0) + b) = x * 1 + (y + b) := by
  intros x y b
  ring

theorem generated_90918 : ∀ x : Nat, (x + x + 0) * 1 = 1 * x * 0 + x * 1 + x := by
  intros x
  ring

theorem generated_90919 : ∀ x : Nat, x * (x + x + (13 + 41 + x)) = x * (x + x + (17 + 0 + 37 + x * 1)) := by
  intros x
  ring

theorem generated_90920 : ∀ x : Nat, (x * 1 + 0) * x + x * 42 + x + 1 * x = x * x + (x * 42 + 0) + x + x := by
  intros x
  ring

theorem generated_90921 : ∀ x y : Nat, 1 * (x * y) + x + x + (0 + 32) = (0 + x * 1) * y + x + (0 + x + 0) + (20 + 12) := by
  intros x y
  ring

theorem generated_90922 : ∀ x y : Nat, x * 1 * y + x = (0 + x) * (y + 0) + x := by
  intros x y
  ring

theorem generated_90923 : ∀ x y z : Nat, x + (1 * y + z) = x + (y + 0) + z := by
  intros x y z
  ring

theorem generated_90924 : ∀ x : Nat, x * (1 * x * x) * 1 = x * (x * (x * 1)) := by
  intros x
  ring

theorem generated_90925 : ∀ x y : Nat, x * y + 32 = 1 * x * y + 32 := by
  intros x y
  ring

theorem generated_90926 : ∀ x : Nat, 0 + (x + 0) + 51 = x + 51 := by
  intros x
  ring

theorem generated_90927 : ∀ x z y : Nat, (x + z) * (x + (y + 1 * x)) = (x + z) * (1 * x + y + 1 * x) := by
  intros x z y
  ring

theorem generated_90928 : ∀ x : Nat, 0 + x + x = x + x := by
  intros x
  ring

theorem generated_90929 : ∀ x y : Nat, x * y = x * (1 * y) := by
  intros x y
  ring

theorem generated_90930 : ∀ x y : Nat, x + y + 15 + x = x + y + 15 + x := by
  intros x y
  ring

theorem generated_90931 : ∀ x : Nat, x + (12 + 58 + 92) + x + x = x + (70 + 92) + x + x := by
  intros x
  ring

theorem generated_90932 : ∀ x y z : Nat, x + y + 0 + z + x = x * 1 + y + (0 + (z + 0)) + x := by
  intros x y z
  ring

theorem generated_90933 : ∀ x y z : Nat, 1 * x + y + 1 * 0 + z + 0 = (0 + x + y + z) * (1 * 1) := by
  intros x y z
  ring

theorem generated_90934 : ∀ x y : Nat, (x + (57 + 27)) * (x * y) = (x + 84) * x * (1 * 0 + (0 + y)) := by
  intros x y
  ring

theorem generated_90935 : ∀ x y : Nat, x * y + 1 * x + (x + (85 + 10)) = x * y + x + x + 95 := by
  intros x y
  ring

theorem generated_90936 : ∀ x y : Nat, x + (y + x) * 1 + 1 * 97 + y = 0 + x + y + x + (0 + 97 * 1) + y := by
  intros x y
  ring

theorem generated_90937 : ∀ x y : Nat, x + y + 35 = x + (0 + y) + 35 := by
  intros x y
  ring

theorem generated_90938 : ∀ x y z : Nat, x + y + z + z = 1 * x + (y + z) + z := by
  intros x y z
  ring

theorem generated_90939 : ∀ x a : Nat, x + 0 + 0 + a = x + (a + 0) := by
  intros x a
  ring

theorem generated_90940 : ∀ x : Nat, x * (x + x + x) = x * (0 + (x + (0 + (x + x)))) := by
  intros x
  ring

theorem generated_90941 : ∀ x y : Nat, x * y + (5 + y) = 1 * (x * y) + (5 + y) := by
  intros x y
  ring

theorem generated_90942 : ∀ z x y : Nat, 0 + (z * x + x + y) + (z + 41) = z * (x * 1) + (x + y) + (z + 41) := by
  intros z x y
  ring

theorem generated_90943 : ∀ x y : Nat, 0 + (x + y) + 0 + y + (0 + (7 + 11)) = x + (1 * y + (y + (3 + 15))) := by
  intros x y
  ring

theorem generated_90944 : ∀ x y : Nat, (x + (28 + 7)) * (35 * 1 * x * y) + y + 3 = (x + 35) * (35 * (x * y) + 0) + y + 3 := by
  intros x y
  ring

theorem generated_90945 : ∀ x z : Nat, x + (0 + (0 + (1 * 0 + z))) + 42 = x + z + 0 + 0 + 0 + 42 := by
  intros x z
  ring

theorem generated_90946 : ∀ x y z : Nat, x + (y + z) + (x + 2) = 0 + (x + 1 * y + z) + (x + (1 + 1)) := by
  intros x y z
  ring

theorem generated_90947 : ∀ x y z : Nat, x + (y + 0) + (0 + z) + x = x + y + z + x := by
  intros x y z
  ring

theorem generated_90948 : ∀ x y : Nat, (0 + x) * (1 * y + 1 * (1 * (0 * 1))) = x * (0 + (0 + (0 + (y + 0)))) := by
  intros x y
  ring

theorem generated_90949 : ∀ x y : Nat, 0 + x * y + 8 + y + x = x * (1 * (1 * 1 * y)) + 8 + y + x := by
  intros x y
  ring

theorem generated_90950 : ∀ x y : Nat, x + y = x + y := by
  intros x y
  ring

theorem generated_90951 : ∀ x y : Nat, x * y = 1 * (x * y) := by
  intros x y
  ring

theorem generated_90952 : ∀ x : Nat, 0 + (x + (x + x)) + x + (98 + x) = x + (1 * (1 * (x + x)) + 0) + x + (98 + x) := by
  intros x
  ring

theorem generated_90953 : ∀ x : Nat, (0 + 0 + x + x) * 1 + 20 + x + x = 1 * (x + x * (1 * 1)) + 20 + x + x := by
  intros x
  ring

theorem generated_90954 : ∀ x y z : Nat, 0 + (x + y) + z + 59 * 1 = x + 0 + y + z + 59 := by
  intros x y z
  ring

theorem generated_90955 : ∀ x y z : Nat, x + (y + z) = (x + y + z) * 1 := by
  intros x y z
  ring

theorem generated_90956 : ∀ x : Nat, x * 1 = 1 * (0 + x * 1) := by
  intros x
  ring

theorem generated_90957 : ∀ x : Nat, 1 * x + x + (x + 75) = x + (x + 0) + (x + 5 * 15) := by
  intros x
  ring

theorem generated_90958 : ∀ x : Nat, 1 * 1 * x = (0 + (x + 0)) * 1 := by
  intros x
  ring

theorem generated_90959 : ∀ x : Nat, x * x + 63 * (x + 0) + 0 = x * (x + 0) + (7 + 56) * x := by
  intros x
  ring

theorem generated_90960 : ∀ x : Nat, 1 * x * x * 1 + x + 69 + (31 + 2) = x * x + x + 69 + 33 := by
  intros x
  ring

theorem generated_90961 : ∀ x y : Nat, 1 * 95 * (1 * 1 * x + y) + 16 + 84 = 95 * (x * 1 + y) + (16 + 84) := by
  intros x y
  ring

theorem generated_90962 : ∀ x y z : Nat, x + y + z + y + 18 + z + z = x + y + z + y + (0 + (14 + 4) + z) + z := by
  intros x y z
  ring

theorem generated_90963 : ∀ x y : Nat, 0 + (0 + 0 + x) + y + 48 = x + y + 48 := by
  intros x y
  ring

theorem generated_90964 : ∀ x y z : Nat, 0 + (x + y + z) + 67 = x + 0 + 0 + (y + z) + 67 := by
  intros x y z
  ring

theorem generated_90965 : ∀ x : Nat, x * x = 0 + x * x := by
  intros x
  ring

theorem generated_90966 : ∀ x y a : Nat, x * y + 83 + a = x * y + 83 + a := by
  intros x y a
  ring

theorem generated_90967 : ∀ x y : Nat, x + 1 * (y + 0) = x + y := by
  intros x y
  ring

theorem generated_90968 : ∀ x y : Nat, (x + y) * 1 + (y + y) = 1 * (x + y) + (0 + y + y) := by
  intros x y
  ring

theorem generated_90969 : ∀ x y : Nat, 0 + (0 + (1 * (x * y) + (y + x * 1)) + y) = x * y + y + (x + 0) + y := by
  intros x y
  ring

theorem generated_90970 : ∀ x y : Nat, x * ((0 + x) * y + y + x) = x * (x * y + 1 * y + x) := by
  intros x y
  ring

theorem generated_90971 : ∀ x : Nat, 1 * (x * x) + (x + x) = (0 + (x + (0 + 0))) * (x + 0) + x + x := by
  intros x
  ring

theorem generated_90972 : ∀ x : Nat, x + x = 0 + (x + x) := by
  intros x
  ring

theorem generated_90973 : ∀ x y : Nat, x * (0 + 1 * y) + 69 = x * ((y + 1 * 0) * (1 + 0) * 1) + (40 + 29) := by
  intros x y
  ring

theorem generated_90974 : ∀ x : Nat, (0 + x + x) * 1 = x + (x * 1 * 1 + 0) := by
  intros x
  ring

theorem generated_90975 : ∀ x y : Nat, 0 + (x + y) + 26 = 1 * x + (y + 12) + (5 + 9) := by
  intros x y
  ring

theorem generated_90976 : ∀ x : Nat, x * x + 24 = x * x + 1 * 24 := by
  intros x
  ring

theorem generated_90977 : ∀ x y z : Nat, 0 + 0 + (0 + 1 * (x + (y + z))) = x + (y + z * 1) := by
  intros x y z
  ring

theorem generated_90978 : ∀ x y b : Nat, (x + 1 * 0) * ((y + (0 + 0)) * 1) + b = (x + 0) * y + b := by
  intros x y b
  ring

theorem generated_90979 : ∀ x z : Nat, x + (1 * (0 + 2) + 0) + 33 + (32 + z) = 1 * (0 + (x + 2)) + 33 + (32 + z) := by
  intros x z
  ring

theorem generated_90980 : ∀ y x : Nat, y * (1 * (x * (x * y)) + x + x) = y * (x * (x * y) + (x + x)) := by
  intros y x
  ring

theorem generated_90981 : ∀ x y : Nat, x + y + 0 = x + y := by
  intros x y
  ring

theorem generated_90982 : ∀ x y z : Nat, x + y + z + 0 + 0 + z + z = x + (y + z) + z + z := by
  intros x y z
  ring

theorem generated_90983 : ∀ x y : Nat, 1 * (x + 0) + (1 + (0 + (0 + (0 + 0)))) * (y + 0) = x + y := by
  intros x y
  ring

theorem generated_90984 : ∀ x : Nat, (0 + x) * x + 42 + 77 + x = x * (x * 1) + 42 + (0 + 77) * 1 + x := by
  intros x
  ring

theorem generated_90985 : ∀ x y : Nat, x * 1 + x + y = x + x + y := by
  intros x y
  ring

theorem generated_90986 : ∀ x y : Nat, 1 * (1 * x) + y = x + y * 1 := by
  intros x y
  ring

theorem generated_90987 : ∀ x y b : Nat, x * y + b = x * y + b := by
  intros x y b
  ring

theorem generated_90988 : ∀ x y z : Nat, x + y * 1 + z = 1 * (x + y) + z := by
  intros x y z
  ring

theorem generated_90989 : ∀ x : Nat, (x + 0) * 1 = 0 * 1 + 0 + x * 1 * 1 := by
  intros x
  ring

theorem generated_90990 : ∀ x : Nat, ((x * x + 1 * 0) * 1 + 4 * 16 * x) * 1 + x = x * x + (28 + 36) * x + x := by
  intros x
  ring

theorem generated_90991 : ∀ x : Nat, x * (1 * x) = (0 + x) * 1 * (x * 1) := by
  intros x
  ring

theorem generated_90992 : ∀ x y z : Nat, (x + (y + (z + (z + 3 * 26)))) * 1 + y = x + y + z + (z + 78) + y := by
  intros x y z
  ring

theorem generated_90993 : ∀ x : Nat, x * (x + 0) * 1 = 1 * (x * x) := by
  intros x
  ring

theorem generated_90994 : ∀ x y : Nat, x + y = 0 + x + y := by
  intros x y
  ring

theorem generated_90995 : ∀ x y : Nat, x * (0 + y) * (1 * (1 + 0)) = 0 + x * (1 * y) := by
  intros x y
  ring

theorem generated_90996 : ∀ x y : Nat, x * y + (71 + 9) + 13 = x * (0 + y) + (10 + 0 + 83) := by
  intros x y
  ring

theorem generated_90997 : ∀ x y : Nat, x * (0 + y) = x * (y + 0) := by
  intros x y
  ring

theorem generated_90998 : ∀ x y z : Nat, x * (1 * y + 1 * 0) + y + z + (x + 78) = 0 + x * y + (0 + y) + z + (x + 78) := by
  intros x y z
  ring

theorem generated_90999 : ∀ x y : Nat, (x + 0) * y + 0 + x = x * 1 * y * 1 + x := by
  intros x y
  ring

theorem generated_91000 : ∀ x y : Nat, x * y * (0 + 1) + y = x * 1 * 1 * y + y := by
  intros x y
  ring
